# Copia este archivo a .env para pruebas locales (.env está en .gitignore,
# nunca se sube al repo). En Render, estas mismas variables se configuran en
# Settings -> Environment del servicio, no acá.

SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu-service-role-key
SUPABASE_BUCKET=gafetes
